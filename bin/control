#!/bin/bash

$PIDFILE=${OPENSHIFT_TMP_DIR}/seafile.pid

case $! in
  start )
    while true ; do 
      nc -l -p 8080 -c 'echo -e "HTTP/1.1 200 OK\n\n $(date)"'
    done &
    echo $! > $PIDFILE
  ;;
  stop )
    xargs -a $PIDFILE
    rm $PIDFILE
  ;;
esac
